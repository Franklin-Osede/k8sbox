apiVersion: v1
kind: Service
metadata:
  name: {{ include "hpa-custom-metrics.fullname" . }}
  labels:
    {{- include "hpa-custom-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hpa-custom-metrics.selectorLabels" . | nindent 4 }}


