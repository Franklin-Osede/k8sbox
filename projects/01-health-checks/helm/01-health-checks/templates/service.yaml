apiVersion: v1
kind: Service
metadata:
  name: {{ include "health-checks.fullname" . }}
  labels:
    {{- include "health-checks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "health-checks.selectorLabels" . | nindent 4 }}

