apiVersion: v1
kind: Service
metadata:
  name: {{ include "configmap-reload.fullname" . }}
  labels:
    {{- include "configmap-reload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "configmap-reload.selectorLabels" . | nindent 4 }}
