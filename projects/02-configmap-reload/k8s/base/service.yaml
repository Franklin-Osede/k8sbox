apiVersion: v1
kind: Service
metadata:
  name: configmap-reload
  labels:
    app: configmap-reload
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: configmap-reload

