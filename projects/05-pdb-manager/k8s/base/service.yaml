apiVersion: v1
kind: Service
metadata:
  name: pdb-manager
  labels:
    app: pdb-manager
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: pdb-manager

