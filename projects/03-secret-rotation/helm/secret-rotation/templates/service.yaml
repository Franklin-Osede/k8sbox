apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-rotation.fullname" . }}
  labels:
    {{- include "secret-rotation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-rotation.selectorLabels" . | nindent 4 }}

