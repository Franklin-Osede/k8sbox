apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - statefulset.yaml
  - cronjob-backup.yaml
  - rbac.yaml

commonLabels:
  app: statefulset-database
  project: k8sbox

