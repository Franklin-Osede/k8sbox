apiVersion: apps/v1
kind: Deployment
metadata:
  name: statefulset-database
  labels:
    app: statefulset-database
spec:
  replicas: 2
  selector:
    matchLabels:
      app: statefulset-database
  template:
    metadata:
      labels:
        app: statefulset-database
    spec:
      serviceAccountName: statefulset-database
      containers:
      - name: statefulset-database
        image: statefulset-database:latest
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: PORT
          value: "3000"
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health/startup
            port: http
          initialDelaySeconds: 0
          periodSeconds: 10
          failureThreshold: 30

